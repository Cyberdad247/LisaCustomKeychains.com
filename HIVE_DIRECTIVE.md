# 🐝 HIVE DIRECTIVE: OPERATION KINETIC WEAVER

**Target:** Create `src/app/customize/page.tsx`
**Priority:** HIGH
**Persona:** Syntax + Octem

## 📝 Specifications
1.  **Route:** `/customize`
2.  **Core Feature:** A "Build Your Own Keychain" interface.
3.  **UI Component:** a `KeychainBuilder` component that allows selecting:
    -   **Color** (Thread color)
    -   **Charm** (Sports ball vs. Simple)
    -   **Text** (Custom name input)
4.  **Governance:**
    -   Must use `framer-motion` for interactions.
    -   Must align with `TASK.md` (Atomic Execution).
    -   Must integrate with Shopify `addToCart` (mocked for now).

## 🔨 Execution Steps
1.  Create directory `src/app/customize`.
2.  Create `page.tsx` with metadata.
3.  Create `src/components/KeychainBuilder.tsx`.
4.  Update `Navbar` to link to `/customize`.

*Execute immediately.*
