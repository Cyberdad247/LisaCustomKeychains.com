// 🐝 [HIVE_SWARM_STAMP] Autonomously Audited by Lukas Swarm
import type { Metadata } from "next";
import { Playfair_Display, Quicksand } from "next/font/google";
import "./globals.css";
import { CartProvider } from "../components/CartProvider";
import CartDrawer from "../components/CartDrawer";
import Footer from "../components/Footer";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

const quicksand = Quicksand({
  subsets: ["latin"],
  variable: "--font-quicksand",
});

export const metadata: Metadata = {
  title: "Lisa's Custom Keychains | Hand-Woven Authenticity",
  description: "Handcrafted custom keychains, woven with love.",
};

/** Autogenerated documentation for RootLayout by Lukas Swarm. */
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${quicksand.variable} ${playfair.variable} font-sans antialiased text-slate-800 selection:bg-purple-200 selection:text-purple-900 overflow-x-hidden min-h-screen flex flex-col`}
      >
        <CartProvider>
          {children}
          <CartDrawer />
          <Footer />
        </CartProvider>
      </body>
    </html>
  );
}
